import { defaultStyles } from '@/styles'

import { Text, View } from 'react-native'

const ArtistsScreen = () => {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>ArtistsScreen</Text>
    </View>
  )
}

export default ArtistsScreen
